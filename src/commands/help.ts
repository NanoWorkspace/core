import Command from "../app/Command"

const command: Command = {
  name: "help",
  call: ({ message, commands }) => {
    // todo: use command.description and command.examples
  },
}

module.exports = command
